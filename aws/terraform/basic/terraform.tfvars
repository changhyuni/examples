# terraform.tfvars
# terraform 실행 시 주석 해체
# aws_region      = "ap-northeast-2" # 배포할 AWS 리전
# base_cidr_block = "10.0.0.0"       # VPC의 기본 CIDR 블록
# vpc_name        = "my-vpc"         # VPC의 이름 태그
# environment     = "staging"        # 배포 환경 (예: development, staging, production)